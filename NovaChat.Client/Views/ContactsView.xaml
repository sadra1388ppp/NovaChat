<UserControl x:Class="NovaChat.Client.Views.ContactsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource AppBackgroundBrush}" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <Button Content="← Back" Width="80" Height="36" Click="BackButton_Click" DockPanel.Dock="Left"/>
            <TextBlock Text="Contacts" FontSize="28" FontWeight="Bold" Margin="20,0,0,0" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
        </DockPanel>

        <TextBlock Grid.Row="1" Text="Find people" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>

        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchTextBox" Height="42" Padding="12" VerticalContentAlignment="Center" FontSize="14" KeyDown="SearchTextBox_KeyDown"/>
            <Button Grid.Column="1" Content="Search" Height="42" Margin="10,0,0,0" Click="SearchButton_Click"/>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Search results" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
            <ItemsControl Grid.Row="1" x:Name="SearchResultsList" Margin="0,0,0,15">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12" Margin="0,0,0,6" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="75"/>
                                    <ColumnDefinition Width="75"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="{Binding UserId}" Margin="0,3,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                    <TextBlock Text="{Binding Email}" Margin="0,2,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="Add" Width="60" Height="30" Click="AddSearchResultButton_Click" HorizontalAlignment="Right"/>
                                <Button Grid.Column="2" Content="Chat" Width="60" Height="30" Click="ChatButton_Click" HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Grid.Row="2" Text="My contacts" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ContactsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15" Margin="0,0,0,8" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="75"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBlock Text="{Binding UserId}" Margin="0,4,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <TextBlock Text="{Binding Email}" Margin="0,2,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="Chat" Width="60" Height="32" Click="ChatContactButton_Click" HorizontalAlignment="Right"/>
                                    <Button Grid.Column="2" Content="Remove" Width="75" Height="32" Click="RemoveButton_Click" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <TextBlock x:Name="StatusText" Grid.Row="4" Margin="0,15,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
    </Grid>
</UserControl>