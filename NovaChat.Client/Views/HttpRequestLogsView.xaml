<UserControl x:Class="NovaChat.Client.Views.HttpRequestLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackgroundBrush}">
    <UserControl.Resources>
        <Style x:Key="LogDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource AppBackgroundBrush}"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="EnableColumnVirtualization" Value="True"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="8,7"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="HTTP Requests"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextBrush}"/>
                <TextBlock Text="All indexed records from aaa.HttpRequests"
                           Margin="0,7,0,0"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>

            <Button x:Name="ReindexButton"
                    Grid.Column="1"
                    Content="↻  Reindex"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Height="42"
                    Margin="12,0,0,0"
                    Padding="14,0"
                    Click="ReindexButton_Click"/>

            <Button Grid.Column="2"
                    Content="←  Back to Chat"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Height="42"
                    Margin="12,0,0,0"
                    Padding="14,0"
                    Click="BackButton_Click"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Height="48"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14">
                <Grid>
                    <TextBlock Text="⌕"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="20"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <TextBox x:Name="SearchBox"
                             Margin="44,0,12,0"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{DynamicResource TextBrush}"
                             VerticalContentAlignment="Center"
                             FontSize="13"
                             ToolTip="Search username, path, status code, IP, response body..."
                             KeyDown="SearchBox_KeyDown"/>
                </Grid>
            </Border>

            <Button x:Name="SearchButton"
                    Grid.Column="1"
                    Content="Search"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Height="48"
                    MinWidth="110"
                    Margin="12,0,0,0"
                    Click="SearchButton_Click"/>

            <Border Grid.Column="2"
                    Margin="12,0,0,0"
                    Height="48"
                    MinWidth="120"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="14,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="ResultCountText"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextBrush}"
                               Text="0"/>
                    <TextBlock Text="MATCHES"
                               FontSize="9"
                               FontWeight="Bold"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Margin="10,6,10,8">
                    <TextBlock Text="HttpRequests"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock x:Name="StatusText"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </Grid>

                <DataGrid x:Name="LogsGrid"
                          Grid.Row="1"
                          Style="{StaticResource LogDataGridStyle}"
                          SelectionChanged="LogsGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                        <DataGridTextColumn Header="RequestId" Binding="{Binding RequestId}" Width="160"/>
                        <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="90"/>
                        <DataGridTextColumn Header="Scheme" Binding="{Binding Scheme}" Width="80"/>
                        <DataGridTextColumn Header="Host" Binding="{Binding Host}" Width="170"/>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="260"/>
                        <DataGridTextColumn Header="QueryString" Binding="{Binding QueryString}" Width="220"/>
                        <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="95"/>
                        <DataGridTextColumn Header="StatusCode" Binding="{Binding StatusCode}" Width="95"/>
                        <DataGridCheckBoxColumn Header="IsAuthenticated" Binding="{Binding IsAuthenticated}" Width="125"/>
                        <DataGridTextColumn Header="UserId" Binding="{Binding UserId}" Width="90"/>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                        <DataGridTextColumn Header="IpAddress" Binding="{Binding IpAddress}" Width="125"/>
                        <DataGridTextColumn Header="UserAgent" Binding="{Binding UserAgent}" Width="280"/>
                        <DataGridTextColumn Header="RequestContentType" Binding="{Binding RequestContentType}" Width="190"/>
                        <DataGridTextColumn Header="RequestContentLength" Binding="{Binding RequestContentLength}" Width="150"/>
                        <DataGridTextColumn Header="ResponseContentType" Binding="{Binding ResponseContentType}" Width="190"/>
                        <DataGridTextColumn Header="ResponseContentLength" Binding="{Binding ResponseContentLength}" Width="160"/>
                        <DataGridTextColumn Header="ResponseBody" Binding="{Binding ResponseBody}" Width="420"/>
                        <DataGridTextColumn Header="StartedAt" Binding="{Binding StartedAtText}" Width="155"/>
                        <DataGridTextColumn Header="CompletedAt" Binding="{Binding CompletedAtText}" Width="155"/>
                        <DataGridTextColumn Header="DurationMs" Binding="{Binding DurationMs}" Width="105"/>
                        <DataGridCheckBoxColumn Header="Succeeded" Binding="{Binding Succeeded}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock x:Name="EmptyText"
                           Grid.Row="1"
                           Text="No HTTP requests found."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Visibility="Collapsed"/>

                <TextBlock Grid.Row="2"
                           Text="Horizontal scroll is available for the complete MariaDB-style column set."
                           Margin="8,9,8,2"
                           FontSize="10"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>