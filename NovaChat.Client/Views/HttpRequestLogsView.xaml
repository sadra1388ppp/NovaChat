<UserControl x:Class="NovaChat.Client.Views.HttpRequestLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackgroundBrush}">
    <UserControl.Resources>
        <Style x:Key="LogDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource AppBackgroundBrush}"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="34">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="HTTP Request Search"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextBrush}"/>
                <TextBlock Text="Search indexed HTTP request logs from the aaa database."
                           Margin="0,7,0,0"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>

            <Button x:Name="ReindexButton"
                    Grid.Column="1"
                    Content="↻  Reindex"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Height="42"
                    Margin="12,0,0,0"
                    Click="ReindexButton_Click"/>

            <Button Grid.Column="2"
                    Content="←  Back to Chat"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Height="42"
                    Margin="12,0,0,0"
                    Click="BackButton_Click"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Height="50"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14">
                <Grid>
                    <TextBlock Text="⌕"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="20"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <TextBox x:Name="SearchBox"
                             Margin="44,0,12,0"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{DynamicResource TextBrush}"
                             VerticalContentAlignment="Center"
                             FontSize="13"
                             ToolTip="Search username, path, status code, IP, response body..."
                             KeyDown="SearchBox_KeyDown"/>
                </Grid>
            </Border>

            <Button x:Name="SearchButton"
                    Grid.Column="1"
                    Content="Search"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Height="50"
                    MinWidth="110"
                    Margin="12,0,0,0"
                    Click="SearchButton_Click"/>

            <Border Grid.Column="2"
                    Margin="12,0,0,0"
                    Height="50"
                    MinWidth="120"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="14,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="ResultCountText"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextBrush}"
                               Text="0"/>
                    <TextBlock Text="RESULTS"
                               FontSize="9"
                               FontWeight="Bold"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Margin="10,8,10,10">
                        <TextBlock Text="HTTP Requests"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}"/>
                        <TextBlock x:Name="StatusText"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                    </Grid>

                    <DataGrid x:Name="LogsGrid"
                              Grid.Row="1"
                              Style="{StaticResource LogDataGridStyle}"
                              SelectionChanged="LogsGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="68"/>
                            <DataGridTextColumn Header="TIME" Binding="{Binding StartedAtText}" Width="150"/>
                            <DataGridTextColumn Header="METHOD" Binding="{Binding Method}" Width="85"/>
                            <DataGridTextColumn Header="STATUS" Binding="{Binding StatusCode}" Width="72"/>
                            <DataGridTextColumn Header="PATH" Binding="{Binding Path}" Width="*"/>
                            <DataGridTextColumn Header="USER" Binding="{Binding Username}" Width="110"/>
                            <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="125"/>
                            <DataGridTextColumn Header="MS" Binding="{Binding DurationMs}" Width="68"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock x:Name="EmptyText"
                               Grid.Row="1"
                               Text="No HTTP requests found."
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="14"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Visibility="Collapsed"/>

                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="8,10,8,4">
                        <Button x:Name="PreviousButton"
                                Content="‹  Previous"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Height="36"
                                Padding="14,0"
                                Margin="0,0,8,0"
                                IsEnabled="False"
                                Click="PreviousButton_Click"/>
                        <TextBlock x:Name="PageText"
                                   VerticalAlignment="Center"
                                   MinWidth="80"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   FontSize="12"/>
                        <Button x:Name="NextButton"
                                Content="Next  ›"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Height="36"
                                Padding="14,0"
                                Margin="8,0,0,0"
                                IsEnabled="False"
                                Click="NextButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1"
                    Margin="18,0,0,0"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="24">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Request Details"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextBrush}"/>

                        <TextBlock x:Name="SelectedRequestText"
                                   Text="Select a request to inspect its details."
                                   Margin="0,5,0,18"
                                   FontSize="12"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   TextWrapping="Wrap"/>

                        <Border Padding="14"
                                Background="{DynamicResource AppBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="14">
                            <StackPanel>
                                <TextBlock Text="REQUEST"
                                           FontSize="9"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock x:Name="RequestMethodText"
                                           Margin="0,7,0,0"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextBrush}"
                                           TextWrapping="Wrap"/>
                                <TextBlock x:Name="RequestPathText"
                                           Margin="0,4,0,0"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextBrush}"
                                           TextWrapping="Wrap"/>
                                <TextBlock x:Name="RequestQueryText"
                                           Margin="0,4,0,0"
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Margin="0,12,0,0"
                                Padding="14"
                                Background="{DynamicResource AppBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="14">
                            <StackPanel>
                                <TextBlock Text="RESULT"
                                           FontSize="9"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock x:Name="ResponseStatusText"
                                           Margin="0,7,0,0"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock x:Name="ResponseContentTypeText"
                                           Margin="0,4,0,0"
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"/>
                                <TextBlock x:Name="DurationText"
                                           Margin="0,4,0,0"
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Margin="0,12,0,0"
                                Padding="14"
                                Background="{DynamicResource AppBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="14">
                            <StackPanel>
                                <TextBlock Text="CLIENT"
                                           FontSize="9"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock x:Name="UsernameText"
                                           Margin="0,7,0,0"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextBrush}"
                                           TextWrapping="Wrap"/>
                                <TextBlock x:Name="IpAddressText"
                                           Margin="0,4,0,0"
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"/>
                                <TextBlock x:Name="UserAgentText"
                                           Margin="0,4,0,0"
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Margin="0,12,0,0"
                                Padding="14"
                                Background="{DynamicResource AppBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="14">
                            <StackPanel>
                                <TextBlock Text="RESPONSE BODY"
                                           FontSize="9"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock x:Name="ResponseBodyText"
                                           Margin="0,7,0,0"
                                           FontSize="11"
                                           Foreground="{DynamicResource TextBrush}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
